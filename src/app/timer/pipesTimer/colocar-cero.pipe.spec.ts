import { ColocarCeroPipe } from './colocar-cero.pipe';

describe('ColocarCeroPipe', () => {
  it('create an instance', () => {
    const pipe = new ColocarCeroPipe();
    expect(pipe).toBeTruthy();
  });
});
